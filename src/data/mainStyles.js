import colors from './colors';

const mainStyles = {
  btn: {
    backgroundColor: colors.blue,
    width: 300,
    height: 50,
    borderRadius: 20,
    alignItems: 'center',
    justifyContent: 'center',
    marginRight: 'auto',
    marginLeft: 'auto',
    marginTop: 'auto',
    marginBottom: 20,
  },
  btnText: {
    color: '#fff',
    fontWeight: '700',
  },
};

export default mainStyles;
